const autoClose = ['input'];
const selfClose = ['hr', 'br'];

module.exports = function toElement(string) {
  let i = 0;
  let n = string.length;
  for (; i < n; i++) {
    if (string[i] === '<') {
      
    }
  }
};
